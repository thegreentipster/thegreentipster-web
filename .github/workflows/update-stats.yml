name: Actualizar estadísticas de Blogabet

on:
  schedule:
    # Todos los días a las 07:00 UTC (~08:00/09:00 hora española según horario de verano)
    - cron: "0 7 * * *"
  workflow_dispatch: {}   # permite lanzarlo manualmente desde la pestaña Actions

permissions:
  contents: write

jobs:
  update-stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: pip install requests beautifulsoup4

      - name: Ejecutar scraper
        run: python3 scripts/scrape_blogabet.py

      - name: Commitear cambios si los hay
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet -- stats.json history.json; then
            git add stats.json history.json
            git commit -m "Actualizar estadísticas de Blogabet [automático]"
            git push
          else
            echo "Sin cambios en stats.json / history.json"
          fi
